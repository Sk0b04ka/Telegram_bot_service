image: python:3.12-slim

build_python:
  stage: build
  script:
    - echo "Build stage example"
  only:
    - merge_requests
    - master

lint_python:
  stage: validate
  script:
    - echo "Lint stage"
   - black .
   - ruff check .
   - pytest --dead-fixtures --dup-fixtures
  only:
    - merge_requests
    - master